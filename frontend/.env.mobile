# Mobile build environment — replace the URL with your actual Render deployment URL.
# Run `npm run mobile:build` (not `npm run build`) when building for Android/iOS.
VITE_API_URL=https://your-tow-arbiter.onrender.com
