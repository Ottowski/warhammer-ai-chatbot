services:
  - type: web
    name: tow-arbiter-api
    env: python
    buildCommand: pip install -r requirements.server.txt
    startCommand: uvicorn api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false   # You will enter this value in the Render dashboard
